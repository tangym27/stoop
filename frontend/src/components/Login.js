import React from "react";
import styled from "styled-components";

const EmailComponent = styled.div``;
const PasswordComponent = styled.div``;

const Login = () => {
  return (
    <section>
      <EmailComponent>Hey</EmailComponent>
      <PasswordComponent>Hewerfy</PasswordComponent>
    </section>
  );
};

export default Login;
